/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['fatimacollection.s3.us-east-1.amazonaws.com',"lh3.googleusercontent.com"],
    },
  };

export default nextConfig;
