export const metadata = {
    title: "Terms and Conditions",
    description: "Terms and Conditions of George Cleaning Group, a cleaning services provider in Australia",
  };
  
  export default function RootLayout({ children }) {
    return (
      <section>
        {children}
      </section>
    );
  }
  